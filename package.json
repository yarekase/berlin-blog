{
  "name": "berlin-blog-monorepo",
  "private": true,
  "workspaces": [
    "front",
    "cfserver"
  ],
  "scripts": {
    "dev:front": "npm run dev --workspace=front",
    "dev:back": "npm run dev --workspace=cfserver",
    "build:front": "npm run build --workspace=front",
    "install:all": "npm install"
  }
}
